# Powerlevel10k Configuration
# ----------------------------------------------
# 顯示 Icon
typeset -g POWERLEVEL9K_OS_ICON_CONTENT_EXPANSION=''

# 使用 nerd font 時可以顯示更多 icon。詳情請參考 powerlevel9k wiki 
POWERLEVEL10K_MODE='nerdfont-complete'

# 左側
# POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(os_icon context dir dir_writable vcs vi_mode)
typeset -g POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(os_icon context dir dir_writable vcs vi_mode)
# 右側
typeset -g POWERLEVEL9K_RIGHT_PROMPT_ELEMENTS=(status background_jobs history ram load time)

# 若當前登入的帳號為你的帳號 seraphwu，就不用特別顯示出來
typeset -g DEFAULT_USER="seraphwu"

# Uncomment the following line to disable configuration wizard
typeset -g POWERLEVEL9K_DISABLE_CONFIGURATION_WIZARD=true
